{
  "permissions": {
    "allow": ["Bash(gh issue *)"]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'SESSION START (genzen): Orient from the two durable surfaces before anything else -- the README `## Status` block (last shipped / up next) and `gh issue list`. Greet the user in 2-3 lines summarizing where the project is, then ask whether to continue that work. There is no continuation file; do not look for one. If they decline or open a different task, drop it.'"
          }
        ]
      }
    ]
  }
}
