node_modules
.DS_Store
*.local
.env
.firecrawl/

.claude/worktrees/
.claude/launch.json

# Next build output (#168 port)
.next/

# TypeScript incremental build cache -- dirties the tree on every typecheck
tsconfig.tsbuildinfo

# Next regenerates this, and rewrites it differently after `dev` than after
# `build`, so tracking it means a permanently dirty tree. Verified: `pnpm
# typecheck` passes without it.
next-env.d.ts
