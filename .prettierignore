package-lock.json
pnpm-lock.yaml
yarn.lock

# Regenerated by `next build` on every run, in its own formatting. Without this,
# `pnpm check` and `pnpm build` fight over the file and it shows up dirty in
# every commit.
next-env.d.ts